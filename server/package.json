{
  "name": "server",
  "type": "module",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start:docker": "NODE_ENV=docker node index.js",
    "start:docker:win": "set NODE_ENV=docker && node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "dev:docker": "NODE_ENV=docker nodemon index.js",
    "dev:docker:win": "set NODE_ENV=docker && nodemon index.js",
    "dev:win": "set NODE_ENV=development && nodemon index.js",
    "prod": "NODE_ENV=production node index.js",
    "prod:win": "set NODE_ENV=production && node index.js",
    "db:init": "node scripts/init-db.js",
    "db:reset": "node scripts/init-db.js",
    "db:init:dev": "NODE_ENV=development node scripts/docker-init-database.js",
    "db:init:dev:win": "set NODE_ENV=development && node scripts/docker-init-database.js",
    "db:init:docker": "NODE_ENV=docker node scripts/docker-init-database.js",
    "db:init:docker:win": "set NODE_ENV=docker && node scripts/docker-init-database.js",
    "users:init": "NODE_ENV=development node scripts/init-users.js",
    "users:init:win": "set NODE_ENV=development && node scripts/init-users.js",
    "users:init:docker": "NODE_ENV=docker node scripts/init-users.js",
    "users:init:docker:win": "set NODE_ENV=docker && node scripts/init-users.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-validator": "^7.2.1",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "multer": "^2.0.0",
    "mysql2": "^3.14.1",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0",
    "sequelize": "^6.37.7",
    "server": "file:",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
