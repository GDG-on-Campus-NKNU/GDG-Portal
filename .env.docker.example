# Docker 環境專用配置文件
# 在 Docker 容器中運行時使用此配置
# 複製此檔案為 .env.docker 並填入實際值

NODE_ENV=production
PORT=3000
CLIENT_URL=http://localhost:3000
OAUTH_CALLBACK_URL=http://localhost:3000/api/auth/google/redirect

# Google OAuth Configuration
# 從 Google Cloud Console 獲取這些值
GOOGLE_CLIENT_ID=your_google_oauth_client_id
GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret

# Docker MySQL Database Configuration
DB_HOST=mysql
DB_PORT=3306
DB_NAME=gdg_portal
DB_USER=gdg_admin
DB_PASSWORD=your_secure_database_password

# Security Keys (請生成安全的隨機字符串)
# 可以使用: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_key_here
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key_here
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
COOKIE_SECRET=your_cookie_secret_key_here

# Session Configuration
SESSION_SECRET=your_session_secret_key_here
